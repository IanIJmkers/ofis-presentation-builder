/**
 * Default Orchestra brand assets.
 *
 * Drop the matching files into `public/` under these exact names — they will
 * automatically appear on every slide that doesn't have a per-presentation
 * logo upload set.
 *
 *   public/orchestra-logo.png        // dark navy wordmark, used on white slides
 *   public/orchestra-logo-white.png  // white wordmark, used on dark slides (title, dividers)
 *   public/orchestra-mark.png        // small icon mark — used as favicon
 */

export const DEFAULT_LOGO_DARK = '/OrchestraBlue.svg';
export const DEFAULT_LOGO_WHITE = '/OrchestraWhite.svg';
export const DEFAULT_LOGO_MARK = '/r.png';
